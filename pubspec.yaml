name: server_manager_app
description: A Flutter mobile app for managing dedicated and bare metal servers.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.3.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  http: ^1.2.2
  intl: ^0.19.0
  path: ^1.9.0
  sqflite: ^2.3.3+1
  uuid: ^4.4.2
  flutter_local_notifications: ^21.0.0
  timezone: ^0.11.0

dev_dependencies:
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
